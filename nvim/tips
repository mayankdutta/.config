can use 'source %' to see the immediate results instead of closing and then reopening.
better use f(for find) then character, instead of typing w again and again, for ex. fw will point to all w's in line and then press ;(for fwd) ,(for bkwd)
